pwd = 'print working directory'
hostname = "my computer's network name"
mkdir = 'make directory'
cd = 'change directory'
ls = 'list directory'
rmdir = 'remove directory'
pushd = 'push directory'
popd = 'pop directory'
cp = 'copy a file or directory'
mv = 'move a file or directory'
less = 'page through a file'
cat = 'print the whole file'
xargs = 'execute arguments'
find = 'find files'
grep = 'find things inside files'
man = 'read a manual page'
apropos = 'find what man page is appropriate'
env = 'look at your environment'
echo = 'print some arguments'
export = 'export/set a new environment variable'
exit = 'exit the shell'
sudo = 'DANGER! become super user root DANGER!'

puts "pwd is the command line of #{pwd}"
puts "hostname is the command line of #{hostname}"
puts "mkdir is the command line of #{mkdir}"
puts "cd is the command line of #{cd}"
puts "ls is the command line of #{ls}"
puts "rmdir is the command line of #{rmdir}"
puts "pushd is the command line of #{pushd}"
puts "popd is the command line of #{popd}"
puts "cp is the command line of #{cp}"
puts "mv is the command line of #{mv}"
puts "less is the command line of #{less}"
puts "cat is the command line of #{cat}"
puts "xargs is the command line of #{xargs}"
puts "find is the command line of #{find}"
puts "grep is the command line of #{grep}"
puts "man is the command line of #{man}"
puts "apropos is the command line of #{apropos}"
puts "env is the command line of #{env}"
puts "echo is the command line of #{echo}"
puts "export is the command line of #{export}"
puts "exit is the command line of #{exit}"
puts "sudo is the command line of #{sudo}"
